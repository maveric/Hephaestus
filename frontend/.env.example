# Frontend Configuration

# Backend API Host IP
# Set this to the IP address where the backend is running
# Examples:
#   - localhost (for local development)
#   - 192.168.1.100 (for access from other devices on your network)
#   - your-domain.com (for production deployment)
VITE_API_HOST=localhost

# Backend API Port
# Default is 8000 (matches the MCP server default port)
VITE_API_PORT=8000

# API Protocol (http or https)
VITE_API_PROTOCOL=http
